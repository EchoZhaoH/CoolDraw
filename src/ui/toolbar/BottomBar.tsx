const BottomBar = () => {
  return (
    <footer className="floating-bar floating-bar--bottom">
      <span className="floating-label">100%</span>
      <span className="floating-label">0, 0</span>
    </footer>
  );
};

export default BottomBar;
